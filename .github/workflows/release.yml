name: Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      tag:
        description: >-
          Tag to publish (e.g. v0.1.0). Must already exist on the repository.
          Leave empty to rehearse against the selected branch: the version is then
          read from Cargo.toml and nothing is published.
        required: false
        type: string
        default: ''
      dry_run:
        description: >-
          With a tag named: build and check everything but skip cargo publish
          and the GitHub Release/asset steps.
        required: false
        type: boolean
        default: false

# Default to least-privilege: only the jobs that write to the repo
# (`gh release create` / `gh release upload`) request `contents: write`.
permissions:
  contents: read

env:
  CARGO_NET_RETRY: "10"
  # What every checkout in this workflow points at: the dispatched tag when one is named,
  # the pushed ref (or the branch a rehearsal was started from) otherwise. Declared once so
  # the six checkouts cannot drift apart.
  RELEASE_REF: >-
    ${{ (github.event_name == 'workflow_dispatch' && inputs.tag != '' && inputs.tag)
    || github.ref }}

concurrency:
  # Spelled out rather than using RELEASE_REF: the `env` context is not available in
  # `concurrency`, which is evaluated before any job starts.
  group: >-
    release-${{ (github.event_name == 'workflow_dispatch' && inputs.tag != '' && inputs.tag)
    || github.ref }}
  cancel-in-progress: false

jobs:
  # Seconds of shell against half an hour of matrix: whether the tag, the manifest, the two
  # lockfiles and the CHANGELOG agree is knowable before anything is compiled. Everything else
  # waits on this job, so a forgotten `cargo update -p hunkpick` or an unclosed CHANGELOG
  # section shows up immediately instead of after four platforms have built archives labelled
  # with a version that will never exist.
  verify-metadata:
    name: Verify tag, manifest, locks and CHANGELOG
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    permissions:
      contents: read
    outputs:
      tag: ${{ steps.tag.outputs.tag }}
      version: ${{ steps.tag.outputs.version }}
      # "Does this run publish?", resolved once. The steps and the job that must not run twice
      # read this instead of each repeating the expression that decides it.
      publish: ${{ steps.tag.outputs.publish }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ env.RELEASE_REF }}

      - name: Resolve tag and version
        # Single source of truth for "what are we releasing". Both push and
        # workflow_dispatch resolve here; later jobs use the same composite action
        # (or read this job's outputs) instead of re-deriving them.
        id: tag
        uses: ./.github/actions/resolve-release-tag
        with:
          tag: ${{ inputs.tag }}
          dry_run: ${{ inputs.dry_run }}

      - name: Verify tag matches Cargo.toml and Cargo.lock versions
        env:
          TAG_VERSION: ${{ steps.tag.outputs.version }}
        run: |
          # cargo pkgid emits e.g. `path+file:///work/repo#0.1.0` or
          # `...#hunkpick@0.1.0`. Strip everything up to the last `#`/`@` to
          # get the resolved package version straight from Cargo's parser,
          # instead of an ad-hoc pattern that could match a `version = "..."`
          # line in a [dependencies.*] block. `--offline` because the lockfile in the tagged
          # tree already resolves everything: no registry, no toolchain setup, no cache.
          CARGO_VERSION=$(cargo pkgid --offline | sed -E 's/.*[#@]//')
          if [ "$TAG_VERSION" != "$CARGO_VERSION" ]; then
            echo "::error::Tag v$TAG_VERSION does not match Cargo.toml version $CARGO_VERSION"
            exit 1
          fi
          # Both lockfiles resolve the crate itself. If the author forgot to refresh one after
          # bumping Cargo.toml, the tag ships with a stale lock; `--locked` would later catch
          # the published one with a confusing "package not found". The fuzz workspace is not
          # published, but its lock is part of the tagged tree, and a stale copy makes every
          # cargo command in `fuzz/` rewrite it and leave the working tree dirty. Fail loudly
          # here instead.
          for lock in Cargo.lock fuzz/Cargo.lock; do
            LOCK_VERSION=$(awk '/^name = "hunkpick"$/ {
              getline; sub(/^version = "/, ""); sub(/"$/, ""); print; exit
            }' "$lock")
            if [ "$LOCK_VERSION" != "$TAG_VERSION" ]; then
              echo "::error::$lock version $LOCK_VERSION does not match tag" \
                "v$TAG_VERSION. Run \`cargo update -p hunkpick\` in that workspace" \
                "and commit the lockfile."
              exit 1
            fi
          done

      - name: Verify CHANGELOG has the version section
        # Through `env:`, like every other step here: the value never reaches the shell as
        # template text, so the step is safe on its own rather than relying on the tag
        # validator's regex staying strict.
        env:
          VER: ${{ steps.tag.outputs.version }}
        run: bash scripts/check-changelog.sh "$VER"

  # Re-run the CI gates on the tagged commit. release.yml is triggered by a
  # tag push, not a branch push, so CI's success on master is not
  # authoritative here. Publication is gated on
  # `needs: [test, lint, msrv, semver, package-binaries]` below; any failing pre-publish job
  # aborts cargo publish before it can ship a broken release. Order of the pipeline:
  # verify-metadata -> test/lint/msrv/semver -> package-binaries (build + verify every archive) ->
  # publish (crates.io + GitHub Release) -> upload-assets. Everything reversible happens
  # before the one step that is not.
  test:
    name: Test (${{ matrix.os }})
    needs: [verify-metadata]
    runs-on: ${{ matrix.os }}
    timeout-minutes: 20
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        # Pinned LTS runners, not `ubuntu-latest` (it trails the newest LTS
        # by months). ubuntu-26.04 is the current Ubuntu LTS (preview runner
        # image as of 2026-06); ubuntu-24.04 stays for stable coverage.
        # windows + macos round out the published-binary platforms.
        os: [ubuntu-24.04, ubuntu-26.04, macos-latest, windows-latest]
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ env.RELEASE_REF }}
      - uses: dtolnay/rust-toolchain@4360b52568e2003a75bf9bc1d59f33a8e3fc893c # stable
      - uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6 # v2.9.2
      # Prebuilt nextest binary: the .config/nextest.toml profile enforces a
      # per-test timeout and bounded parallelism that `cargo test` cannot
      # express. Mirrors the CI test job so the release gate runs the same way.
      - uses: taiki-e/install-action@b6b84cf49ebfe0176417bdce007c624f0db37f20 # v2.86.2
        with:
          tool: cargo-nextest@0.9.143
      - name: Fetch dependencies (with retry)
        uses: ./.github/actions/cargo-fetch-retry
      - name: cargo build
        run: cargo build --verbose
      - name: Run tests
        run: cargo nextest run --all-features
      # nextest does not run doctests; keep them covered explicitly.
      - name: Run doctests
        run: cargo test --all-features --doc

  lint:
    name: Lint (fmt + clippy)
    needs: [verify-metadata]
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ env.RELEASE_REF }}
      - uses: dtolnay/rust-toolchain@4360b52568e2003a75bf9bc1d59f33a8e3fc893c # stable
        with:
          components: clippy, rustfmt
      - uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6 # v2.9.2
      - name: Fetch dependencies (with retry)
        uses: ./.github/actions/cargo-fetch-retry
      - name: cargo fmt --check
        run: cargo fmt --all -- --check
      - name: cargo clippy
        run: cargo clippy --all-targets --all-features -- -D warnings

  msrv:
    name: MSRV build (1.85.0)
    needs: [verify-metadata]
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ env.RELEASE_REF }}
      # rust-toolchain.toml pins the repository to stable and wins over the toolchain the
      # next step installs; without this override the gate `publish` waits on would build on
      # stable, so an MSRV break would reach crates.io behind a green check. See ci.yml.
      - shell: bash
        run: echo "RUSTUP_TOOLCHAIN=1.85.0" >> "$GITHUB_ENV"
      - uses: dtolnay/rust-toolchain@bc540ba06a4ccee415bb241490e0b25ee8e7d315 # 1.85.0
      - uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6 # v2.9.2
      - name: Fetch dependencies (with retry)
        uses: ./.github/actions/cargo-fetch-retry
      # `--all-targets` so the dev-dependencies are compiled too — the `proptest = "~1.11"`
      # pin is an MSRV pin, and `cargo build` alone never reaches it.
      - name: cargo check --locked --all-targets
        run: cargo check --locked --all-targets --verbose

  semver:
    name: Semver (public API)
    needs: [verify-metadata]
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    permissions:
      contents: read
    # Here for the same reason as test/lint/msrv: a tag can sit on a commit CI never saw, or on
    # one whose version was bumped after the last run, and `cargo publish` cannot be undone.
    # The check reads the released version off crates.io and reports any incompatibility the
    # bump does not cover, under 0.x semantics — a breaking change requires a minor bump.
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ env.RELEASE_REF }}
      - uses: dtolnay/rust-toolchain@4360b52568e2003a75bf9bc1d59f33a8e3fc893c # stable
      - uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6 # v2.9.2
      - uses: taiki-e/install-action@b6b84cf49ebfe0176417bdce007c624f0db37f20 # v2.86.2
        with:
          tool: cargo-semver-checks@0.50.0
      - name: Check the public API against the released version
        run: cargo semver-checks check-release

  publish:
    name: Publish to crates.io
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    # Also waits for package-binaries: `cargo publish` is irreversible (only `cargo yank`
    # afterwards), so every archive must be built and pass the contract check BEFORE the
    # version reaches crates.io. Otherwise a packaging failure leaves a published version
    # whose release has no assets, and `cargo binstall hunkpick` 404s for it.
    needs: [verify-metadata, test, lint, msrv, semver, package-binaries]
    # No approval gate: a tag push publishes. A GitHub Environment with a required reviewer
    # would pause the run before `cargo publish`, at the cost of a manual step per release;
    # the project is small enough that a mistaken tag burning a version number is the cheaper
    # outcome. Add `environment: <name>` here, with the token as that environment's secret,
    # if that ever stops being true (see RELEASING.md).
    # The only jobs that need to write to the repo: `gh release create`
    # requires `contents: write`.
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ env.RELEASE_REF }}
      - uses: dtolnay/rust-toolchain@4360b52568e2003a75bf9bc1d59f33a8e3fc893c # stable
      - uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6 # v2.9.2
      - name: Fetch dependencies (with retry)
        uses: ./.github/actions/cargo-fetch-retry

      - name: Release smoke test
        # Run on the LTO-enabled release profile to catch optimizer-only bugs
        # (UB exposed by inlining, dead-code elimination collapsing a side
        # effect) that the debug-profile tests cannot see. The exit code is
        # the signal of interest, not the output content.
        run: |
          cargo build --release --locked
          ./target/release/hunkpick --version
          ./target/release/hunkpick --help >/dev/null
          printf '%s\n' \
            'diff --git a/f b/f' '--- a/f' '+++ b/f' \
            '@@ -1 +1 @@' '-old' '+new' \
            | ./target/release/hunkpick list >/dev/null

      - name: cargo package --list
        run: cargo package --list

      - name: cargo publish --dry-run
        # No token here, deliberately: a dry run never asks the registry for credentials
        # (verified with a CARGO_HOME holding no credentials.toml and the variable unset).
        # It does run a verification build, which compiles the package and the build scripts
        # and proc macros of its dependencies — all of them inheriting this step's environment.
        # A rehearsal (`workflow_dispatch` with an empty tag) builds whatever branch was chosen
        # in the dialog, so a token here would be a crates.io credential, good for every crate
        # of the account and outliving any repository access, handed to code from an arbitrary
        # branch. The `cargo publish` step below keeps its own copy.
        #
        # --locked forbids Cargo from regenerating Cargo.lock; the published
        # version is whatever was committed at the tagged commit. Pairs with
        # the lock-version check in verify-metadata so a stale lock cannot reach crates.io.
        run: cargo publish --dry-run --locked

      - name: cargo publish
        if: ${{ needs.verify-metadata.outputs.publish == 'true' }}
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}
        run: cargo publish --locked

      - name: Extract release notes from CHANGELOG.md
        if: ${{ needs.verify-metadata.outputs.publish == 'true' }}
        # The tag was resolved and checked by verify-metadata; this job reads its outputs
        # rather than resolving it a second time.
        env:
          VER: ${{ needs.verify-metadata.outputs.version }}
        run: |
          awk -v ver="$VER" '
            $0 ~ "^## \\["ver"\\]" { flag=1; next }
            flag && /^## \[/ { exit }
            flag { print }
          ' CHANGELOG.md | sed -e '1{/^$/d}' -e '$,${/^$/d}' > release-notes.md
          if [ ! -s release-notes.md ]; then
            echo "::warning::No CHANGELOG.md entry found for $VER;" \
              "falling back to a placeholder note."
            echo "_See git history._" > release-notes.md
          fi

      - name: Create GitHub Release
        if: ${{ needs.verify-metadata.outputs.publish == 'true' }}
        env:
          GH_TOKEN: ${{ github.token }}
          TAG: ${{ needs.verify-metadata.outputs.tag }}
        run: |
          if gh release view "$TAG" --repo "$GITHUB_REPOSITORY" >/dev/null 2>&1; then
            echo "Release $TAG already exists, skipping create."
            exit 0
          fi
          gh release create "$TAG" \
            --repo "$GITHUB_REPOSITORY" \
            --title "$TAG" \
            --notes-file release-notes.md \
            --verify-tag

  # Cross-platform prebuilt assets. Runs BEFORE `publish` (which waits on it) so a broken
  # archive cannot follow an irreversible crates.io publication; the archives travel to the
  # upload-assets job as workflow artifacts. Needs no repository write access — it only
  # builds and checks.
  package-binaries:
    name: Package (${{ matrix.target }})
    needs: [verify-metadata, test, lint, msrv]
    runs-on: ${{ matrix.os }}
    timeout-minutes: 30
    permissions:
      contents: read
      # Both are for the provenance attestation at the end of the job: `id-token` to
      # obtain the OIDC token Sigstore signs against, `attestations` to record the
      # signed statement against this repository.
      id-token: write
      attestations: write
    strategy:
      fail-fast: false
      matrix:
        # One tier-1 target per OS. The Linux row stays on the older LTS
        # (ubuntu-24.04) on purpose: linking against its older glibc yields a
        # binary that also runs on newer distributions, whereas building on
        # 26.04 would require a newer glibc at runtime. macos-latest is Apple
        # Silicon; x86_64-apple-darwin is built on macos-15-intel, a standard
        # runner of the same architecture, so that scripts/verify-archive.sh
        # can run the packaged binary. Cross-compiling it on the Apple Silicon
        # runner needed only `rustup target add`, but left one of the four
        # published archives never executed by anything before release.
        include:
          - os: ubuntu-24.04
            target: x86_64-unknown-linux-gnu
            archive: tar.gz
            binary: hunkpick
          - os: macos-latest
            target: aarch64-apple-darwin
            archive: tar.gz
            binary: hunkpick
          - os: macos-15-intel
            target: x86_64-apple-darwin
            archive: tar.gz
            binary: hunkpick
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            archive: zip
            binary: hunkpick.exe
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ env.RELEASE_REF }}
      - uses: dtolnay/rust-toolchain@4360b52568e2003a75bf9bc1d59f33a8e3fc893c # stable
        with:
          targets: ${{ matrix.target }}
      - name: Install GNU tar on macOS
        # scripts/package-archive.sh uses GNU-tar flags (--sort, --owner,
        # --group, --numeric-owner, --mtime) for reproducible archives. The
        # script prefers `gtar`; `gnu-tar` provides it on Homebrew so macOS
        # produces the same byte-for-byte layout as Linux.
        if: runner.os == 'macOS'
        run: brew install gnu-tar
      - uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6 # v2.9.2
        with:
          # Per-target cache key so the matrix entries do not stomp each
          # other's target/ directory.
          key: ${{ matrix.target }}
      - name: Fetch dependencies (with retry)
        uses: ./.github/actions/cargo-fetch-retry

      - name: Resolve tag and version
        # Same composite action as the publish job — the single source of truth
        # for the release tag/version.
        id: tag
        uses: ./.github/actions/resolve-release-tag
        with:
          tag: ${{ inputs.tag }}

      - name: Build release binary
        run: cargo build --release --locked --target ${{ matrix.target }}

      # The released binary statically links third-party crates whose licenses require the
      # copyright notice to travel with the binary, so the notices file is part of the archive
      # contract (scripts/verify-archive.sh enforces its presence).
      - uses: taiki-e/install-action@b6b84cf49ebfe0176417bdce007c624f0db37f20 # v2.86.2
        with:
          tool: cargo-about@0.9.2
      - name: Generate third-party notices
        shell: bash
        run: bash scripts/generate-notices.sh

      # Packaging is delegated to scripts/package-archive.sh so the same
      # logic can be exercised locally. `tar` is available on every runner;
      # `7z` is preinstalled on windows-latest and is the most stable
      # cross-shell way to produce a .zip without PowerShell CRLF gotchas.
      - name: Package archive and SHA-256
        id: pack
        shell: bash
        env:
          VER: ${{ steps.tag.outputs.version }}
          TARGET: ${{ matrix.target }}
          ARCHIVE_EXT: ${{ matrix.archive }}
          BIN_NAME: ${{ matrix.binary }}
          BIN_PATH: target/${{ matrix.target }}/release/${{ matrix.binary }}
        run: bash scripts/package-archive.sh >> "$GITHUB_OUTPUT"

      # Defends the archive contract: filename matches the template, sibling
      # .sha256 verifies cleanly, archive expands to a single top-level
      # directory matching the stem containing exactly the binary, README.md,
      # LICENSE and THIRD-PARTY-NOTICES.md, and the packaged binary runs and
      # reports the version its filename claims. Catches layout regressions
      # before the asset reaches users.
      - name: Verify archive against downstream contract
        shell: bash
        env:
          ASSET: ${{ steps.pack.outputs.asset }}
          BIN_NAME: ${{ matrix.binary }}
        run: bash scripts/verify-archive.sh "$ASSET" "$BIN_NAME"

      # Provenance: a Sigstore-signed statement binding this archive to the workflow run,
      # commit and repository that produced it, verifiable by anyone who downloads it:
      #
      #   gh attestation verify hunkpick-X.Y.Z-<target>.tar.gz --repo VitalyOstanin/hunkpick
      #
      # The .sha256 companion says the download arrived intact; it says nothing about where
      # the bytes came from, and it is served from the same Release page as the archive, so
      # whoever could replace one could replace the other. Attestations are free for public
      # repositories. Only a run that publishes attests: a rehearsal builds an arbitrary
      # branch, and a signed statement about an archive that never ships is noise in the log
      # a verifier reads.
      - name: Attest the archive's provenance
        if: needs.verify-metadata.outputs.publish == 'true'
        uses: actions/attest-build-provenance@4d101475d8b20a2381f78447822ac1eab6504dd8 # v4.2.2
        with:
          subject-path: ${{ steps.pack.outputs.asset }}

      # Hand the verified archive to upload-assets. Keeping the upload in a separate job is
      # what lets this one run before `publish`.
      - name: Upload archive as a workflow artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: archive-${{ matrix.target }}
          path: |
            ${{ steps.pack.outputs.asset }}
            ${{ steps.pack.outputs.asset }}.sha256
          if-no-files-found: error
          # A hand-off between two jobs of the same run, not a record: `upload-assets` attaches
          # it to the Release, which is where it is kept from then on.
          retention-days: 7

  # Attach the already-verified archives to the Release `publish` created. Last step of the
  # pipeline: by now crates.io and the Release both exist, and nothing here can fail in a way
  # that leaves an inconsistent published version behind.
  upload-assets:
    name: Upload release assets
    # `verify-metadata` is in `needs` for its `publish` output: a job reads outputs only from
    # the jobs it declares, and reaching them through `publish` is not enough.
    needs: [verify-metadata, publish, package-binaries]
    if: ${{ needs.verify-metadata.outputs.publish == 'true' }}
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ env.RELEASE_REF }}
      - name: Resolve tag and version
        id: tag
        uses: ./.github/actions/resolve-release-tag
        with:
          tag: ${{ inputs.tag }}
      - name: Download archives
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          pattern: archive-*
          merge-multiple: true
          path: assets
      - name: Upload to GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
          TAG: ${{ steps.tag.outputs.tag }}
        shell: bash
        run: |
          # `--clobber` makes the upload idempotent: a re-run after a
          # transient failure overwrites the partial blob instead of erroring
          # with "asset already exists".
          gh release upload "$TAG" assets/* \
            --repo "$GITHUB_REPOSITORY" \
            --clobber
